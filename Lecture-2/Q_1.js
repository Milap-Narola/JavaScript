/* Q.1 you have to create three numbers, stored in variables with the following names
one, two, three
• You have to find the square of the three numbers and find the sum of their square values
Note: The square of a number, is a number multiplied by itself. For example, the square of 3, is 3 * 3 = 9
input 
1 2 3
output
14 */


let a = 1;
let b = 2;
let c = 3;
let one = a * a;
let two = b * b;
let three = c * c;

console.log(`one = ${a} * ${a} = ${one}`);
console.log(`two = ${b} * ${b} = ${two}`);
console.log(`three = ${c} * ${c} = ${three}`);


let sumSquare = one + two + three;
console.log(`${one} + ${two} + ${three} = ${sumSquare}`);


